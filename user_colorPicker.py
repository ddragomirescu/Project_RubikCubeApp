from CubRubik import CubRubik

def ask_user_for_colors():
    # purpose return a dictionary with user-defined colors for each face
    
    face_names = ['U', 'D', 'F', 'B', 'L', 'R']

    print("Please enter the colors for each face of the Rubik's Cube.")
    user_input = print("Select face with letters U (Up), D (Down), F (Front), B (Back), L (Left), R (Right).")
    

    return user_input